% 第十四章：其他方法简介

\chapter{其他方法简介}

\begin{wulibj}[本章导读]
除了前面介绍的主要方法外，数学物理方程还有许多其他求解方法。本章简要介绍变分法、摄动法和数值方法的基本思想，为后续学习和研究打下基础。
\end{wulibj}

\begin{wulibj}[物理背景]
本章介绍的三种方法在物理学中各有其独特地位：

\textbf{1. 变分法}：源于最小作用量原理——这是物理学最深刻的原理之一。从牛顿力学到量子场论，物理系统的运动方程都可以从作用量泛函的极值导出。费曼曾说："大自然似乎总是选择使作用量取极值的路径。"

\textbf{应用实例}：
\begin{itemize}
    \item 经典力学：最小作用量原理给出拉格朗日方程
    \item 量子力学：变分法是近似求解基态能量的重要方法
    \item 弹性力学：最小势能原理
    \item 电磁学：汤姆逊定理（带电导体的能量最小）
\end{itemize}

\textbf{2. 摄动法}：处理含小参数的问题，在几乎所有物理分支中都有应用。从行星运动的微扰、量子力学中的能级修正，到流体力学中的边界层理论，摄动法提供了从精确解出发系统逼近的方法。

\textbf{3. 数值方法}：现代计算物理的基石。绝大多数实际问题无法求得解析解，数值方法使我们能够模拟从原子到星系的各类物理过程。有限元方法、有限差分法等已成为工程师和物理学家的日常工具。

\textbf{本章目标}：了解这些方法的基本思想，知道它们各自的适用范围，为深入学习奠定基础。
\end{wulibj}

% ========== 第一节 ==========

\subsection{变分原理}

许多物理问题可以表述为某个泛函的极值问题。例如：
\begin{itemize}
    \item 力学中的最小作用量原理
    \item 光学中的费马\index{费马@费马（Fermat）}（Fermat）原理
    \item 静电学中的汤姆逊\index{汤姆逊@汤姆逊（Thomson）}（Thomson）原理
\end{itemize}

\begin{dingliyi}{泛函}{}
设 $J[y]$ 是函数 $y(x)$ 的“函数”，称为\textbf{泛函}。变分法研究泛函的极值问题。
\end{dingliyi}

\subsection{欧拉-拉格朗日方程}

考虑泛函
\begin{equation}
    J[y] = \int_a^b F(x, y, y')\,\mathrm{d}x
\end{equation}

\begin{dingli}{欧拉-拉格朗日方程}{}
使泛函 $J[y]$ 取极值的函数 $y(x)$ 满足
\[
    \frac{\partial F}{\partial y} - \frac{\mathrm{d}}{\mathrm{d}x}\frac{\partial F}{\partial y'} = 0
\]
\end{dingli}

\begin{proof}
设 $y(x)$ 是极值函数，考虑变分 $y + \varepsilon\eta(x)$，其中 $\eta(a) = \eta(b) = 0$。

令
\begin{equation}
    \Phi(\varepsilon) = J[y + \varepsilon\eta] = \int_a^b F(x, y + \varepsilon\eta, y' + \varepsilon\eta')\,\mathrm{d}x
\end{equation}

极值条件：$\Phi'(0) = 0$。

\begin{equation}
    \Phi'(0) = \int_a^b \left(\frac{\partial F}{\partial y}\eta + \frac{\partial F}{\partial y'}\eta'\right)\,\mathrm{d}x = 0
\end{equation}

分部积分第二项：
\begin{equation}
    \int_a^b \frac{\partial F}{\partial y'}\eta'\,\mathrm{d}x = \left[\frac{\partial F}{\partial y'}\eta\right]_a^b - \int_a^b \frac{\mathrm{d}}{\mathrm{d}x}\frac{\partial F}{\partial y'}\eta\,\mathrm{d}x = -\int_a^b \frac{\mathrm{d}}{\mathrm{d}x}\frac{\partial F}{\partial y'}\eta\,\mathrm{d}x
\end{equation}

所以
\begin{equation}
    \int_a^b \left(\frac{\partial F}{\partial y} - \frac{\mathrm{d}}{\mathrm{d}x}\frac{\partial F}{\partial y'}\right)\eta\,\mathrm{d}x = 0
\end{equation}

由于 $\eta$ 任意，得到欧拉-拉格朗日方程。
\end{proof}

\subsection{变分法与微分方程的关系}

许多微分方程可以转化为变分问题。

\begin{lizi}{}{}
证明拉普拉斯方程 $\nabla^2 u = 0$ 对应于泛函 $J[u] = \frac{1}{2}\int_\Omega |\nabla u|^2\,\mathrm{d}V$ 的极小值问题。

\begin{jie}
    $F = \frac{1}{2}|\nabla u|^2 = \frac{1}{2}(u_x^2 + u_y^2 + u_z^2)$。
    
    欧拉-拉格朗日方程：
    \begin{equation}
        \frac{\partial F}{\partial u} - \sum_{i}\frac{\partial}{\partial x_i}\frac{\partial F}{\partial u_{x_i}} = 0 - \nabla^2 u = 0
    \end{equation}
    
    即 $\nabla^2 u = 0$。
    
    物理意义：调和函数使``能量泛函''最小。
\end{jie}
\end{lizi}

\subsection{里茨方法}

变分法可以用于数值求解：选择一组基函数 $\{\phi_n\}$，设近似解 $u \approx \sum_{n=1}^N c_n\phi_n$，代入泛函 $J[u]$，对系数 $c_n$ 求极值，得到线性方程组。

\begin{figure}[htbp]
\centering
\begin{tikzpicture}[scale=0.8]
    % 坐标轴
    \draw[->, gray] (-0.3, 0) -- (5.5, 0) node[right] {$x$};
    \draw[->, gray] (0, -0.3) -- (0, 3.5) node[above] {$y$};
    
    % 边界点
    \fill (0.5, 0.8) circle (2pt);
    \fill (5, 2.5) circle (2pt);
    \node[below] at (0.5, 0.7) {$a$};
    \node[below] at (5, 2.4) {$b$};
    
    % 极值函数（实线）
    \draw[blue, thick, domain=0.5:5, samples=50] plot (\x, {0.8 + 0.38*(\x-0.5) - 0.03*(\x-2.75)^2});
    
    % 其他函数（虚线）
    \draw[red, dashed, domain=0.5:5, samples=50] plot (\x, {0.8 + 0.48*(\x-0.5) - 0.02*(\x-2.75)^2});
    \draw[red, dashed, domain=0.5:5, samples=50] plot (\x, {0.8 + 0.30*(\x-0.5) + 0.05*(\x-2.75)^2});
    \draw[red, dashed, domain=0.5:5, samples=50] plot (\x, {0.8 + 0.38*(\x-0.5) + 0.08*sin((\x-0.5)*60)});
    
    % 标注
    \node[blue, right] at (5.1, 2.2) {$y(x)$（极值）};
    \node[red, right] at (5.1, 2.9) {$y + \varepsilon\eta$};
    
    % 变分示意
    \draw[<->, green!60!black, thick] (3, 1.95) -- (3, 2.35);
    \node[green!60!black, right] at (3.1, 2.15) {$\delta y$};
    
    \node at (2.75, -0.8) {泛函极值示意图};
\end{tikzpicture}
\caption{变分法示意：在函数空间中寻找使泛函取极值的函数}
\label{fig:variational}
\end{figure}

% ========== 第二节 ==========
\section{摄动法}

\subsection{正则摄动}

当方程含有小参数 $\varepsilon$ 时，可以将解展开为 $\varepsilon$ 的幂级数。

\begin{lizi}{}{}
求解摄动方程：
\begin{equation}
    y'' + y = \varepsilon y^2, \quad y(0) = 1, \quad y'(0) = 0
\end{equation}

\begin{jie}
    设 $y = y_0 + \varepsilon y_1 + \varepsilon^2 y_2 + \cdots$。
    
    代入方程，比较 $\varepsilon$ 的各阶系数：
    
    $O(1)$：
    \begin{equation}
        y_0'' + y_0 = 0, \quad y_0(0) = 1, \quad y_0'(0) = 0
    \end{equation}
    解得 $y_0 = \cos x$。
    
    $O(\varepsilon)$：
    \begin{equation}
        y_1'' + y_1 = y_0^2 = \cos^2 x, \quad y_1(0) = y_1'(0) = 0
    \end{equation}
    
    利用 $\cos^2 x = \frac{1 + \cos 2x}{2}$：
    \begin{equation}
        y_1'' + y_1 = \frac{1}{2} + \frac{1}{2}\cos 2x
    \end{equation}
    
    特解：$y_{1p} = \frac{1}{2} - \frac{1}{6}\cos 2x$。
    
    通解：$y_1 = A\cos x + B\sin x + \frac{1}{2} - \frac{1}{6}\cos 2x$。
    
    由初始条件得 $A = -\frac{1}{3}$，$B = 0$。
    
    所以
    \begin{equation}
        y \approx \cos x + \varepsilon\left(\frac{1}{2} - \frac{1}{3}\cos x - \frac{1}{6}\cos 2x\right)
    \end{equation}
\end{jie}
\end{lizi}

\subsection{奇异摄动}

当小参数出现在最高阶导数前时，正则摄动可能失效。这时需要使用\textbf{边界层方法}。

\begin{jinshi}[边界层现象]
当最高阶导数系数为小参数时，解可能在边界附近发生剧烈变化，形成``边界层''。正则展开无法捕捉这种行为。
\end{jinshi}

\subsection{多尺度方法}

对于长期项（随时间增长的项），可以使用多尺度方法：引入多个时间尺度 $T_0 = t$，$T_1 = \varepsilon t$，$T_2 = \varepsilon^2 t$，$\ldots$。

\begin{figure}[htbp]
\centering
\begin{tikzpicture}[scale=0.85]
    % 坐标轴
    \draw[->, gray] (-0.3, 0) -- (6.5, 0) node[right] {$x$};
    \draw[->, gray] (0, -1.5) -- (0, 2.5) node[above] {$y$};
    
    % 基本解 y0 = cos(x)
    \draw[blue, thick, domain=0:6, samples=80] plot (\x, {cos(\x r)});
    \node[blue, right] at (6.1, 0.8) {$y_0(x)$};
    
    % 摄动解 epsilon = 0.2
    \draw[red, dashed, thick, domain=0:6, samples=80] plot (\x, {cos(\x r) + 0.2*(0.5 - 0.33*cos(\x r) - 0.17*cos(2*\x r))});
    \node[red, right] at (6.1, 0.3) {$y_0 + \varepsilon y_1$};
    
    % 摄动解 epsilon = 0.4
    \draw[orange, dotted, thick, domain=0:6, samples=80] plot (\x, {cos(\x r) + 0.4*(0.5 - 0.33*cos(\x r) - 0.17*cos(2*\x r))});
    \node[orange, right] at (6.1, -0.2) {$\varepsilon = 0.4$};
    
    % 标注
    \node at (3, -2) {摄动法示意：小参数对解的影响};
    
    % epsilon 标注
    \draw[<->, green!60!black, thick] (2, -0.42) -- (2, -0.02);
    \node[green!60!black, left] at (1.9, -0.22) {\small $\varepsilon y_1$};
\end{tikzpicture}
\caption{摄动法示意：随着小参数 $\varepsilon$ 增大，解偏离基解 $y_0$}
\label{fig:perturbation}
\end{figure}

% ========== 第三节 ==========
\section{数值方法简介}

\subsection{有限差分法}

将偏导数用差商近似：
\begin{align}
    \frac{\partial u}{\partial x} &\approx \frac{u_{i+1,j} - u_{i-1,j}}{2\Delta x} \\
    \frac{\partial^2 u}{\partial x^2} &\approx \frac{u_{i+1,j} - 2u_{i,j} + u_{i-1,j}}{(\Delta x)^2}
\end{align}

将微分方程离散化为代数方程组。

\begin{figure}[htbp]
\centering
\begin{tikzpicture}[scale=0.8]
    % 网格
    \draw[gray, thin] (0, 0) grid (5, 4);
    
    % 点
    \foreach \i in {0,...,5} {
        \foreach \j in {0,...,4} {
            \fill (\i, \j) circle (1.5pt);
        }
    }
    
    % 中心点及其邻居
    \fill[red] (2.5, 2) circle (3pt);
    \fill[blue] (1.5, 2) circle (3pt);
    \fill[blue] (3.5, 2) circle (3pt);
    \fill[blue] (2.5, 1) circle (3pt);
    \fill[blue] (2.5, 3) circle (3pt);
    
    \node[red, below] at (2.5, 1.8) {$u_{i,j}$};
    \node[blue, left] at (1.3, 2) {$u_{i-1,j}$};
    \node[blue, right] at (3.7, 2) {$u_{i+1,j}$};
    \node[blue, below] at (2.5, 0.8) {$u_{i,j-1}$};
    \node[blue, above] at (2.5, 3.2) {$u_{i,j+1}$};
    
    \node at (2.5, -0.7) {五点差分格式};
\end{tikzpicture}
\caption{有限差分网格}
\label{fig:finite-difference}
\end{figure}

\subsection{有限元法}

将求解区域划分为有限个小单元，在每个单元内用插值函数近似解，通过变分原理得到离散方程。

基本步骤：
\begin{enumerate}
    \item 区域剖分
    \item 选择插值函数
    \item 单元分析
    \item 整体合成
    \item 施加边界条件
    \item 求解线性方程组
\end{enumerate}

\begin{figure}[htbp]
\centering
\begin{tikzpicture}[scale=0.75]
    % 不规则区域边界
    \draw[thick, blue] (0, 0) -- (5, 0) -- (5.5, 2) -- (4, 3.5) -- (1.5, 3) -- (0, 1.5) -- cycle;
    
    % 三角形单元网格
    % 底层
    \draw[gray, thin] (0, 0) -- (2, 0) -- (1, 1) -- cycle;
    \draw[gray, thin] (2, 0) -- (3.5, 0) -- (2.5, 1) -- cycle;
    \draw[gray, thin] (3.5, 0) -- (5, 0) -- (4, 1) -- cycle;
    
    % 中下层
    \draw[gray, thin] (1, 1) -- (2, 0) -- (2.5, 1) -- cycle;
    \draw[gray, thin] (2.5, 1) -- (3.5, 0) -- (4, 1) -- cycle;
    \draw[gray, thin] (1, 1) -- (2.5, 1) -- (2, 2) -- cycle;
    \draw[gray, thin] (2.5, 1) -- (4, 1) -- (3, 2) -- cycle;
    
    % 中层
    \draw[gray, thin] (0, 1.5) -- (1, 1) -- (2, 2) -- cycle;
    \draw[gray, thin] (2, 2) -- (2.5, 1) -- (3, 2) -- cycle;
    \draw[gray, thin] (3, 2) -- (4, 1) -- (4.5, 2) -- cycle;
    
    % 中上层
    \draw[gray, thin] (1.5, 3) -- (2, 2) -- (3, 2.5) -- cycle;
    \draw[gray, thin] (3, 2.5) -- (3, 2) -- (4, 2.5) -- cycle;
    \draw[gray, thin] (4, 2.5) -- (4.5, 2) -- (5.5, 2) -- cycle;
    
    % 顶层
    \draw[gray, thin] (1.5, 3) -- (3, 2.5) -- (4, 3.5) -- cycle;
    \draw[gray, thin] (3, 2.5) -- (4, 2.5) -- (4, 3.5) -- cycle;
    \draw[gray, thin] (4, 2.5) -- (5.5, 2) -- (4, 3.5) -- cycle;
    
    % 节点
    \foreach \x/\y in {0/0, 2/0, 3.5/0, 5/0, 1/1, 2.5/1, 4/1, 0/1.5, 2/2, 3/2, 4.5/2, 5.5/2, 1.5/3, 3/2.5, 4/2.5, 4/3.5} {
        \fill (\x, \y) circle (1.5pt);
    }
    
    % 标注一个单元
    \draw[red, thick] (2.5, 1) -- (3.5, 0) -- (4, 1) -- cycle;
    \node[red] at (3.3, 0.5) {\small $e_k$};
    
    \node at (2.75, -0.8) {三角形单元剖分};
\end{tikzpicture}
\caption{有限元网格示意：将区域划分为三角形单元}
\label{fig:fem-mesh}
\end{figure}

\subsection{有限体积法}

基于积分形式的守恒定律，对每个控制体积建立离散方程。特别适合流体力学问题。

% ========== 第四节 ==========
\section{方法总结与选择}

\begin{table}[htbp]
\centering
\begin{tabular}{llll}
\toprule
\textbf{方法} & \textbf{适用方程类型} & \textbf{优点} & \textbf{缺点} \\
\midrule
行波法 & 波动方程 & 解析解，物理意义明确 & 只适用于初值问题 \\
分离变量法\index{分离变量法} & 有界区域问题 & 理论完整 & 只适用于规则区域 \\
积分变换法 & 无界/半无界问题 & 自动处理无穷远条件 & 逆变换困难 \\
格林函数\index{格林函数}法 & 线性问题 & 解的结构清晰 & 格林函数\index{格林函数}难求 \\
变分法 & 椭圆型问题 & 理论基础坚实 & 需要找到正确泛函 \\
摄动法 & 含小参数问题 & 渐近展开 & 可能出现长期项 \\
数值方法 & 一般问题 & 适用范围广 & 精度依赖网格 \\
\bottomrule
\end{tabular}
\caption{各种方法的比较}
\end{table}

% ========== 本章小结 ==========
\section{本章小结}

\subsection{变分法}

\begin{itemize}
    \item 泛函极值 $\Rightarrow$ 欧拉-拉格朗日方程
    \item 微分方程 $\Leftrightarrow$ 变分问题
    \item 应用：里茨方法、伽辽金方法
\end{itemize}

\subsection{摄动法}

\begin{itemize}
    \item 正则摄动：幂级数展开
    \item 奇异摄动：边界层方法
    \item 多尺度方法：消除长期项
\end{itemize}

\subsection{数值方法}

\begin{itemize}
    \item 有限差分法：简单直观
    \item 有限元法：适应复杂边界
    \item 有限体积法：保持守恒性
\end{itemize}

% ========== 习题 ==========
\section{习题}

\textbf{基础题}

\begin{enumerate}
    \item 推导泛函 $J[y] = \int_a^b (y'^2 + y^2)\,\mathrm{d}x$ 的欧拉-拉格朗日方程。
    
    \item 用正则摄动法求解 $y' + y = \varepsilon y^2$，$y(0) = 1$。
\end{enumerate}

\textbf{综合题}

\begin{enumerate}[resume]
    \item 证明：对于泛函 $J[u] = \int_\Omega \left[\frac{1}{2}|\nabla u|^2 - fu\right]\,\mathrm{d}V$，其欧拉-拉格朗日方程是泊松方程 $-\nabla^2 u = f$。
    
    \item 用有限差分法写出泊松方程 $u_{xx} + u_{yy} = f$ 在正方形网格上的五点差分格式。
\end{enumerate}

\textbf{挑战题}

\begin{enumerate}[resume]
    \item 研究奇异摄动问题 $\varepsilon y'' + y' + y = 0$，$y(0) = 0$，$y(1) = 1$ 的边界层结构。
\end{enumerate}
